# -----------------------------------------------------------------------------
# CONFIGURACIÓN DE APLICACIÓN
# -----------------------------------------------------------------------------
NODE_ENV=development
PORT=3000
APP_NAME="Mi Increíble App"

# -----------------------------------------------------------------------------
# BASE DE DATOS (PostgreSQL)
# -----------------------------------------------------------------------------
# Ejemplo: postgresql://USER:PASSWORD@localhost:5432/DB_NAME?schema=public
DATABASE_URL=

# -----------------------------------------------------------------------------
# SEGURIDAD JWT (Mínimo 32 caracteres cada una)
# -----------------------------------------------------------------------------
# Tip: Puedes generar una clave con: openssl rand -base64 32
JWT_SECRET=
JWT_REFRESH_SECRET=

# Tiempos de expiración en segundos
JWT_ACCESS_EXPIRES_IN=3600
JWT_REFRESH_EXPIRES_IN=2592000

# -----------------------------------------------------------------------------
# CONFIGURACIÓN DE CORS
# -----------------------------------------------------------------------------
# Lista separada por comas sin espacios
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# -----------------------------------------------------------------------------
# SEGURIDAD ADICIONAL (OTP)
# -----------------------------------------------------------------------------
# Tiempo de vida del código OTP en segundos (ej. 600 = 10 min)
OTP_EXPIRES_IN=600

# -----------------------------------------------------------------------------
# MENSAJERÍA (Twilio) 
# -----------------------------------------------------------------------------
# NOTA: En desarrollo, si estas variables están vacías, los códigos 
# se mostrarán únicamente en la consola del servidor.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=